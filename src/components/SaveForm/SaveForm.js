import React from "react";
import { Form, Button, Alert } from "react-bootstrap";

const SaveForm = ({ handleSubmit, handleChange, formData, error }) => {
  return <div></div>;
};

export default SaveForm;
